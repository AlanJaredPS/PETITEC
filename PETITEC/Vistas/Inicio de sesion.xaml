<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PETITEC.Vistas.Inicio_de_sesion"
             NavigationPage.HasNavigationBar="False">

    <Grid BackgroundColor="#79CDE0">
        <!-- Definición de filas para organizar el contenido -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Contenido del formulario de inicio de sesión -->
        <StackLayout VerticalOptions="Center" 
                     HorizontalOptions="FillAndExpand" 
                     Padding="30">

            <!-- Título -->
            <Label Text="Iniciar Sesión"
                   FontAttributes="Bold"
                   FontSize="35"
                   TextColor="White"
                   HorizontalOptions="Center"/>

            <!-- Campo de entrada para el correo electrónico o usuario -->
            <Entry Placeholder="Correo o Usuario"
                   Keyboard="Email"
                   FontSize="20"
                   x:Name="entryEmailUsuario"
                   Margin="0,20,0,10"
                   TextColor="White"
                   PlaceholderColor="White"/>

            <!-- Campo de entrada para la contraseña -->
            <Entry Placeholder="Contraseña"
                   IsPassword="True"
                   FontSize="20"
                   x:Name="entryPassword"
                   Margin="0,10,0,30"
                   TextColor="White"
                   PlaceholderColor="White"/>

            <!-- Botón de Iniciar Sesión -->
            <Button Text="Iniciar Sesión"
                    FontSize="25"
                    FontAttributes="Bold"
                    CornerRadius="30"
                    BackgroundColor="#D9FFDA18"
                    Clicked="BtnIniciarSesion_Clicked"/>
        </StackLayout>
    </Grid>

</ContentPage>